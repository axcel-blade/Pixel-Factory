---
interface Props {
  href: string;
  src: string;
  alt: string;
}

const { href, src, alt } = Astro.props;
---

<a href={href} class="mt-2 shrink-0 transition-opacity hover:opacity-80">
  <img src={src} alt={alt} width="100" height="100" class="h-auto w-20 sm:w-24" />
</a>
